import { Dependencies, CodeBlock } from "@/components/docs/component-installation"

# Install Next.js

Create a new Next.js project with TypeScript, Tailwind CSS, ESLint, and App Router.

---

<Dependencies step={1} title="Create Project" copyText="npx create-next-app@latest my-app --typescript --tailwind --eslint --app --src-dir --import-alias '@/*'">
Run the following command to create a new Next.js project:

<CodeBlock 
  code="npx create-next-app@latest my-app --typescript --tailwind --eslint --app --src-dir --import-alias '@/*'"
  language="bash"
/>

Navigate to your project directory:

<CodeBlock 
  code="cd my-app"
  language="bash"
/>
</Dependencies>
